<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes">

    <!-- Material Design 3 Input Controls -->
    <!-- Based on MD3 text field specs: https://m3.material.io/components/text-fields/overview -->

    <!-- MD3 Filled Text Field Style -->
    <Style x:Key="MD3FilledTextFieldStyle" TargetType="{x:Type TextBox}" BasedOn="{StaticResource MaterialDesignFilledTextBox}">
        <Setter Property="FontFamily" Value="{StaticResource PrimaryFontFamily}"/>
        <Setter Property="FontSize" Value="{StaticResource BodyLargeFontSize}"/>
        <Setter Property="FontWeight" Value="{StaticResource BodyFontWeight}"/>
        <Setter Property="Foreground" Value="{DynamicResource OnSurfaceBrush}"/>
        <Setter Property="Background" Value="{DynamicResource SurfaceVariantBrush}"/>
        <Setter Property="BorderThickness" Value="0,0,0,1"/>
        <Setter Property="BorderBrush" Value="{DynamicResource OnSurfaceVariantBrush}"/>
        <Setter Property="Padding" Value="{StaticResource MediumSpacing}"/>
        <Setter Property="Height" Value="{StaticResource FABSize}"/>
        <Setter Property="UseLayoutRounding" Value="True"/>
        <Setter Property="materialDesign:HintAssist.Foreground" Value="{DynamicResource OnSurfaceVariantBrush}"/>
        <Setter Property="materialDesign:HintAssist.FloatingScale" Value="0.75"/>
        <Setter Property="materialDesign:TextFieldAssist.TextFieldCornerRadius" Value="{StaticResource ExtraSmallCornerRadius}"/>
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="BorderBrush" Value="{DynamicResource OnSurfaceBrush}"/>
            </Trigger>
            <Trigger Property="IsKeyboardFocused" Value="True">
                <Setter Property="BorderBrush" Value="{DynamicResource PrimaryBrush}"/>
                <Setter Property="BorderThickness" Value="0,0,0,2"/>
            </Trigger>
            <Trigger Property="Validation.HasError" Value="True">
                <Setter Property="BorderBrush" Value="{DynamicResource ErrorBrush}"/>
                <Setter Property="materialDesign:HintAssist.Foreground" Value="{DynamicResource ErrorBrush}"/>
            </Trigger>
            <Trigger Property="IsEnabled" Value="False">
                <Setter Property="Opacity" Value="0.38"/>
                <Setter Property="materialDesign:HintAssist.Foreground" Value="{DynamicResource OnSurfaceBrush}"/>
            </Trigger>
        </Style.Triggers>
    </Style>

    <!-- MD3 Outlined Text Field Style -->
    <Style x:Key="MD3OutlinedTextFieldStyle" TargetType="{x:Type TextBox}" BasedOn="{StaticResource MaterialDesignOutlinedTextBox}">
        <Setter Property="FontFamily" Value="{StaticResource PrimaryFontFamily}"/>
        <Setter Property="FontSize" Value="{StaticResource BodyLargeFontSize}"/>
        <Setter Property="FontWeight" Value="{StaticResource BodyFontWeight}"/>
        <Setter Property="Foreground" Value="{DynamicResource OnSurfaceBrush}"/>
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="BorderThickness" Value="{StaticResource ThinBorder}"/>
        <Setter Property="BorderBrush" Value="{DynamicResource OutlineBrush}"/>
        <Setter Property="Padding" Value="{StaticResource MediumSpacing}"/>
        <Setter Property="Height" Value="{StaticResource FABSize}"/>
        <Setter Property="UseLayoutRounding" Value="True"/>
        <Setter Property="materialDesign:HintAssist.Foreground" Value="{DynamicResource OnSurfaceVariantBrush}"/>
        <Setter Property="materialDesign:HintAssist.FloatingScale" Value="0.75"/>
        <Setter Property="materialDesign:TextFieldAssist.TextFieldCornerRadius" Value="{StaticResource ExtraSmallCornerRadius}"/>
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="BorderBrush" Value="{DynamicResource OnSurfaceBrush}"/>
            </Trigger>
            <Trigger Property="IsKeyboardFocused" Value="True">
                <Setter Property="BorderBrush" Value="{DynamicResource PrimaryBrush}"/>
                <Setter Property="BorderThickness" Value="{StaticResource MediumBorder}"/>
            </Trigger>
            <Trigger Property="Validation.HasError" Value="True">
                <Setter Property="BorderBrush" Value="{DynamicResource ErrorBrush}"/>
                <Setter Property="materialDesign:HintAssist.Foreground" Value="{DynamicResource ErrorBrush}"/>
            </Trigger>
            <Trigger Property="IsEnabled" Value="False">
                <Setter Property="Opacity" Value="0.38"/>
                <Setter Property="BorderBrush" Value="{DynamicResource OnSurfaceBrush}"/>
                <Setter Property="materialDesign:HintAssist.Foreground" Value="{DynamicResource OnSurfaceBrush}"/>
            </Trigger>
        </Style.Triggers>
    </Style>

    <!-- MD3 Password Field Style -->
    <Style x:Key="MD3PasswordFieldStyle" TargetType="{x:Type PasswordBox}" BasedOn="{StaticResource MaterialDesignFilledPasswordBox}">
        <Setter Property="FontFamily" Value="{StaticResource PrimaryFontFamily}"/>
        <Setter Property="FontSize" Value="{StaticResource BodyLargeFontSize}"/>
        <Setter Property="Foreground" Value="{DynamicResource OnSurfaceBrush}"/>
        <Setter Property="Background" Value="{DynamicResource SurfaceVariantBrush}"/>
        <Setter Property="BorderThickness" Value="0,0,0,1"/>
        <Setter Property="BorderBrush" Value="{DynamicResource OnSurfaceVariantBrush}"/>
        <Setter Property="Padding" Value="{StaticResource MediumSpacing}"/>
        <Setter Property="Height" Value="{StaticResource FABSize}"/>
        <Setter Property="UseLayoutRounding" Value="True"/>
        <Setter Property="materialDesign:HintAssist.Foreground" Value="{DynamicResource OnSurfaceVariantBrush}"/>
        <Setter Property="materialDesign:TextFieldAssist.TextFieldCornerRadius" Value="{StaticResource ExtraSmallCornerRadius}"/>
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="BorderBrush" Value="{DynamicResource OnSurfaceBrush}"/>
            </Trigger>
            <Trigger Property="IsKeyboardFocused" Value="True">
                <Setter Property="BorderBrush" Value="{DynamicResource PrimaryBrush}"/>
                <Setter Property="BorderThickness" Value="0,0,0,2"/>
            </Trigger>
            <Trigger Property="IsEnabled" Value="False">
                <Setter Property="Opacity" Value="0.38"/>
                <Setter Property="materialDesign:HintAssist.Foreground" Value="{DynamicResource OnSurfaceBrush}"/>
            </Trigger>
        </Style.Triggers>
    </Style>

    <!-- MD3 Multiline Text Area Style -->
    <Style x:Key="MD3TextAreaStyle" TargetType="{x:Type TextBox}" BasedOn="{StaticResource MaterialDesignOutlinedTextBox}">
        <Setter Property="FontFamily" Value="{StaticResource PrimaryFontFamily}"/>
        <Setter Property="FontSize" Value="{StaticResource BodyLargeFontSize}"/>
        <Setter Property="FontWeight" Value="{StaticResource BodyFontWeight}"/>
        <Setter Property="Foreground" Value="{DynamicResource OnSurfaceBrush}"/>
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="BorderThickness" Value="{StaticResource ThinBorder}"/>
        <Setter Property="BorderBrush" Value="{DynamicResource OutlineBrush}"/>
        <Setter Property="Padding" Value="{StaticResource MediumSpacing}"/>
        <Setter Property="MinHeight" Value="120"/>
        <Setter Property="TextWrapping" Value="Wrap"/>
        <Setter Property="AcceptsReturn" Value="True"/>
        <Setter Property="VerticalScrollBarVisibility" Value="Auto"/>
        <Setter Property="UseLayoutRounding" Value="True"/>
        <Setter Property="materialDesign:HintAssist.Foreground" Value="{DynamicResource OnSurfaceVariantBrush}"/>
        <Setter Property="materialDesign:HintAssist.FloatingScale" Value="0.75"/>
        <Setter Property="materialDesign:TextFieldAssist.TextFieldCornerRadius" Value="{StaticResource ExtraSmallCornerRadius}"/>
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="BorderBrush" Value="{DynamicResource OnSurfaceBrush}"/>
            </Trigger>
            <Trigger Property="IsKeyboardFocused" Value="True">
                <Setter Property="BorderBrush" Value="{DynamicResource PrimaryBrush}"/>
                <Setter Property="BorderThickness" Value="{StaticResource MediumBorder}"/>
            </Trigger>
            <Trigger Property="IsEnabled" Value="False">
                <Setter Property="Opacity" Value="0.38"/>
            </Trigger>
        </Style.Triggers>
    </Style>

    <!-- MD3 Checkbox Style -->
    <Style x:Key="MD3CheckboxStyle" TargetType="{x:Type CheckBox}" BasedOn="{StaticResource MaterialDesignCheckBox}">
        <Setter Property="FontFamily" Value="{StaticResource PrimaryFontFamily}"/>
        <Setter Property="FontSize" Value="{StaticResource BodyLargeFontSize}"/>
        <Setter Property="FontWeight" Value="{StaticResource BodyFontWeight}"/>
        <Setter Property="Foreground" Value="{DynamicResource OnSurfaceBrush}"/>
        <Setter Property="Padding" Value="{StaticResource SmallSpacing}"/>
        <Setter Property="UseLayoutRounding" Value="True"/>
        <Setter Property="materialDesign:CheckBoxAssist.CheckBoxSize" Value="{StaticResource MediumIconSize}"/>
        <Setter Property="materialDesign:RippleAssist.IsDisabled" Value="False"/>
        <Style.Triggers>
            <Trigger Property="IsEnabled" Value="False">
                <Setter Property="Opacity" Value="0.38"/>
            </Trigger>
        </Style.Triggers>
    </Style>

    <!-- MD3 Radio Button Style -->
    <Style x:Key="MD3RadioButtonStyle" TargetType="{x:Type RadioButton}" BasedOn="{StaticResource MaterialDesignRadioButton}">
        <Setter Property="FontFamily" Value="{StaticResource PrimaryFontFamily}"/>
        <Setter Property="FontSize" Value="{StaticResource BodyLargeFontSize}"/>
        <Setter Property="FontWeight" Value="{StaticResource BodyFontWeight}"/>
        <Setter Property="Foreground" Value="{DynamicResource OnSurfaceBrush}"/>
        <Setter Property="Padding" Value="{StaticResource SmallSpacing}"/>
        <Setter Property="UseLayoutRounding" Value="True"/>
        <Setter Property="materialDesign:RadioButtonAssist.RadioButtonSize" Value="{StaticResource MediumIconSize}"/>
        <Setter Property="materialDesign:RippleAssist.IsDisabled" Value="False"/>
        <Style.Triggers>
            <Trigger Property="IsEnabled" Value="False">
                <Setter Property="Opacity" Value="0.38"/>
            </Trigger>
        </Style.Triggers>
    </Style>

    <!-- MD3 ComboBox Style -->
    <Style x:Key="MD3ComboBoxStyle" TargetType="{x:Type ComboBox}" BasedOn="{StaticResource MaterialDesignFilledComboBox}">
        <Setter Property="FontFamily" Value="{StaticResource PrimaryFontFamily}"/>
        <Setter Property="FontSize" Value="{StaticResource BodyLargeFontSize}"/>
        <Setter Property="FontWeight" Value="{StaticResource BodyFontWeight}"/>
        <Setter Property="Foreground" Value="{DynamicResource OnSurfaceBrush}"/>
        <Setter Property="Background" Value="{DynamicResource SurfaceVariantBrush}"/>
        <Setter Property="BorderThickness" Value="0,0,0,1"/>
        <Setter Property="BorderBrush" Value="{DynamicResource OnSurfaceVariantBrush}"/>
        <Setter Property="Padding" Value="{StaticResource MediumSpacing}"/>
        <Setter Property="Height" Value="{StaticResource FABSize}"/>
        <Setter Property="UseLayoutRounding" Value="True"/>
        <Setter Property="materialDesign:HintAssist.Foreground" Value="{DynamicResource OnSurfaceVariantBrush}"/>
        <Setter Property="materialDesign:HintAssist.FloatingScale" Value="0.75"/>
        <Setter Property="materialDesign:TextFieldAssist.TextFieldCornerRadius" Value="{StaticResource ExtraSmallCornerRadius}"/>
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="BorderBrush" Value="{DynamicResource OnSurfaceBrush}"/>
            </Trigger>
            <Trigger Property="IsKeyboardFocused" Value="True">
                <Setter Property="BorderBrush" Value="{DynamicResource PrimaryBrush}"/>
                <Setter Property="BorderThickness" Value="0,0,0,2"/>
            </Trigger>
            <Trigger Property="IsEnabled" Value="False">
                <Setter Property="Opacity" Value="0.38"/>
            </Trigger>
        </Style.Triggers>
    </Style>

    <!-- MD3 Outlined ComboBox Style -->
    <Style x:Key="MD3OutlinedComboBoxStyle" TargetType="{x:Type ComboBox}" BasedOn="{StaticResource MaterialDesignOutlinedComboBox}">
        <Setter Property="FontFamily" Value="{StaticResource PrimaryFontFamily}"/>
        <Setter Property="FontSize" Value="{StaticResource BodyLargeFontSize}"/>
        <Setter Property="FontWeight" Value="{StaticResource BodyFontWeight}"/>
        <Setter Property="Foreground" Value="{DynamicResource OnSurfaceBrush}"/>
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="BorderThickness" Value="{StaticResource ThinBorder}"/>
        <Setter Property="BorderBrush" Value="{DynamicResource OutlineBrush}"/>
        <Setter Property="Padding" Value="{StaticResource MediumSpacing}"/>
        <Setter Property="Height" Value="{StaticResource FABSize}"/>
        <Setter Property="UseLayoutRounding" Value="True"/>
        <Setter Property="materialDesign:HintAssist.Foreground" Value="{DynamicResource OnSurfaceVariantBrush}"/>
        <Setter Property="materialDesign:HintAssist.FloatingScale" Value="0.75"/>
        <Setter Property="materialDesign:TextFieldAssist.TextFieldCornerRadius" Value="{StaticResource ExtraSmallCornerRadius}"/>
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="BorderBrush" Value="{DynamicResource OnSurfaceBrush}"/>
            </Trigger>
            <Trigger Property="IsKeyboardFocused" Value="True">
                <Setter Property="BorderBrush" Value="{DynamicResource PrimaryBrush}"/>
                <Setter Property="BorderThickness" Value="{StaticResource MediumBorder}"/>
            </Trigger>
            <Trigger Property="IsEnabled" Value="False">
                <Setter Property="Opacity" Value="0.38"/>
            </Trigger>
        </Style.Triggers>
    </Style>

    <!-- MD3 Slider Style -->
    <Style x:Key="MD3SliderStyle" TargetType="{x:Type Slider}" BasedOn="{StaticResource MaterialDesignSlider}">
        <Setter Property="Foreground" Value="{DynamicResource PrimaryBrush}"/>
        <Setter Property="Background" Value="{DynamicResource OutlineBrush}"/>
        <Setter Property="Height" Value="{StaticResource LargeButtonHeight}"/>
        <Setter Property="UseLayoutRounding" Value="True"/>
        <Style.Triggers>
            <Trigger Property="IsEnabled" Value="False">
                <Setter Property="Opacity" Value="0.38"/>
            </Trigger>
        </Style.Triggers>
    </Style>

    <!-- MD3 Switch Style -->
    <Style x:Key="MD3SwitchStyle" TargetType="{x:Type ToggleButton}" BasedOn="{StaticResource MaterialDesignSwitchToggleButton}">
        <Setter Property="UseLayoutRounding" Value="True"/>
        <Setter Property="materialDesign:ToggleButtonAssist.SwitchTrackOnBackground" Value="{DynamicResource PrimaryBrush}"/>
        <Setter Property="materialDesign:ToggleButtonAssist.SwitchTrackOffBackground" Value="{DynamicResource SurfaceVariantBrush}"/>
        <Style.Triggers>
            <Trigger Property="IsEnabled" Value="False">
                <Setter Property="Opacity" Value="0.38"/>
            </Trigger>
        </Style.Triggers>
    </Style>

    <!-- MD3 List Box Style -->
    <Style x:Key="MD3ListBoxStyle" TargetType="{x:Type ListBox}" BasedOn="{StaticResource MaterialDesignListBox}">
        <Setter Property="FontFamily" Value="{StaticResource PrimaryFontFamily}"/>
        <Setter Property="FontSize" Value="{StaticResource BodyLargeFontSize}"/>
        <Setter Property="FontWeight" Value="{StaticResource BodyFontWeight}"/>
        <Setter Property="Foreground" Value="{DynamicResource OnSurfaceBrush}"/>
        <Setter Property="Background" Value="{DynamicResource SurfaceBrush}"/>
        <Setter Property="BorderBrush" Value="{DynamicResource OutlineBrush}"/>
        <Setter Property="BorderThickness" Value="{StaticResource ThinBorder}"/>
        <Setter Property="Padding" Value="{StaticResource ExtraSmallSpacing}"/>
        <Setter Property="UseLayoutRounding" Value="True"/>
        <Setter Property="materialDesign:ListBoxAssist.IsToggle" Value="False"/>
        <Style.Triggers>
            <Trigger Property="IsEnabled" Value="False">
                <Setter Property="Opacity" Value="0.38"/>
            </Trigger>
        </Style.Triggers>
    </Style>

    <!-- MD3 List Box Item Style -->
    <Style x:Key="MD3ListBoxItemStyle" TargetType="{x:Type ListBoxItem}" BasedOn="{StaticResource MaterialDesignListBoxItem}">
        <Setter Property="FontFamily" Value="{StaticResource PrimaryFontFamily}"/>
        <Setter Property="FontSize" Value="{StaticResource BodyLargeFontSize}"/>
        <Setter Property="FontWeight" Value="{StaticResource BodyFontWeight}"/>
        <Setter Property="Foreground" Value="{DynamicResource OnSurfaceBrush}"/>
        <Setter Property="Padding" Value="{StaticResource ItemPadding}"/>
        <Setter Property="Height" Value="{StaticResource FABSize}"/>
        <Setter Property="UseLayoutRounding" Value="True"/>
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background" Value="{DynamicResource SurfaceVariantBrush}"/>
            </Trigger>
            <Trigger Property="IsSelected" Value="True">
                <Setter Property="Background" Value="{DynamicResource PrimaryContainerBrush}"/>
                <Setter Property="Foreground" Value="{DynamicResource OnPrimaryContainerBrush}"/>
            </Trigger>
            <Trigger Property="IsEnabled" Value="False">
                <Setter Property="Opacity" Value="0.38"/>
            </Trigger>
        </Style.Triggers>
    </Style>

    <!-- MD3 Date Picker Style -->
    <Style x:Key="MD3DatePickerStyle" TargetType="{x:Type DatePicker}" BasedOn="{StaticResource MaterialDesignFilledDatePicker}">
        <Setter Property="FontFamily" Value="{StaticResource PrimaryFontFamily}"/>
        <Setter Property="FontSize" Value="{StaticResource BodyLargeFontSize}"/>
        <Setter Property="FontWeight" Value="{StaticResource BodyFontWeight}"/>
        <Setter Property="Foreground" Value="{DynamicResource OnSurfaceBrush}"/>
        <Setter Property="Background" Value="{DynamicResource SurfaceVariantBrush}"/>
        <Setter Property="BorderThickness" Value="0,0,0,1"/>
        <Setter Property="BorderBrush" Value="{DynamicResource OnSurfaceVariantBrush}"/>
        <Setter Property="Padding" Value="{StaticResource MediumSpacing}"/>
        <Setter Property="Height" Value="{StaticResource FABSize}"/>
        <Setter Property="UseLayoutRounding" Value="True"/>
        <Setter Property="materialDesign:HintAssist.Foreground" Value="{DynamicResource OnSurfaceVariantBrush}"/>
        <Setter Property="materialDesign:TextFieldAssist.TextFieldCornerRadius" Value="{StaticResource ExtraSmallCornerRadius}"/>
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="BorderBrush" Value="{DynamicResource OnSurfaceBrush}"/>
            </Trigger>
            <Trigger Property="IsKeyboardFocused" Value="True">
                <Setter Property="BorderBrush" Value="{DynamicResource PrimaryBrush}"/>
                <Setter Property="BorderThickness" Value="0,0,0,2"/>
            </Trigger>
            <Trigger Property="IsEnabled" Value="False">
                <Setter Property="Opacity" Value="0.38"/>
            </Trigger>
        </Style.Triggers>
    </Style>

    <!-- Legacy Compatibility Aliases -->
    <Style x:Key="TextFieldStyle" TargetType="{x:Type TextBox}" BasedOn="{StaticResource MD3FilledTextFieldStyle}"/>
    <Style x:Key="OutlinedTextFieldStyle" TargetType="{x:Type TextBox}" BasedOn="{StaticResource MD3OutlinedTextFieldStyle}"/>
    <Style x:Key="PasswordFieldStyle" TargetType="{x:Type PasswordBox}" BasedOn="{StaticResource MD3PasswordFieldStyle}"/>
    <Style x:Key="TextAreaStyle" TargetType="{x:Type TextBox}" BasedOn="{StaticResource MD3TextAreaStyle}"/>
    <Style x:Key="CheckboxStyle" TargetType="{x:Type CheckBox}" BasedOn="{StaticResource MD3CheckboxStyle}"/>
    <Style x:Key="RadioButtonStyle" TargetType="{x:Type RadioButton}" BasedOn="{StaticResource MD3RadioButtonStyle}"/>
    <Style x:Key="ComboBoxStyle" TargetType="{x:Type ComboBox}" BasedOn="{StaticResource MD3ComboBoxStyle}"/>
    <Style x:Key="SliderStyle" TargetType="{x:Type Slider}" BasedOn="{StaticResource MD3SliderStyle}"/>
    <Style x:Key="SwitchStyle" TargetType="{x:Type ToggleButton}" BasedOn="{StaticResource MD3SwitchStyle}"/>
    <Style x:Key="ListBoxStyle" TargetType="{x:Type ListBox}" BasedOn="{StaticResource MD3ListBoxStyle}"/>
    <Style x:Key="DatePickerStyle" TargetType="{x:Type DatePicker}" BasedOn="{StaticResource MD3DatePickerStyle}"/>

</ResourceDictionary>