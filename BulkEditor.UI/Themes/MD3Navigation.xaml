<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes">

    <!-- Material Design 3 Navigation Components -->
    <!-- Based on MD3 navigation specs: https://m3.material.io/components/navigation-drawer/overview -->

    <!-- MD3 Navigation Rail Style -->
    <Style x:Key="MD3NavigationRailStyle" TargetType="{x:Type Border}">
        <Setter Property="Background" Value="{DynamicResource SurfaceBrush}"/>
        <Setter Property="BorderBrush" Value="{DynamicResource OutlineVariantBrush}"/>
        <Setter Property="BorderThickness" Value="0,0,1,0"/>
        <Setter Property="Width" Value="80"/>
        <Setter Property="Padding" Value="{StaticResource SmallSpacing}"/>
        <Setter Property="UseLayoutRounding" Value="True"/>
        <Setter Property="Effect">
            <Setter.Value>
                <DropShadowEffect Color="Black" Opacity="0.04" BlurRadius="2" ShadowDepth="1"/>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- MD3 Navigation Rail Item Style -->
    <Style x:Key="MD3NavigationRailItemStyle" TargetType="{x:Type Button}">
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="Foreground" Value="{DynamicResource OnSurfaceVariantBrush}"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Width" Value="64"/>
        <Setter Property="Height" Value="64"/>
        <Setter Property="Margin" Value="{StaticResource ExtraSmallSpacing}"/>
        <Setter Property="Padding" Value="{StaticResource SmallSpacing}"/>
        <Setter Property="FontFamily" Value="{StaticResource PrimaryFontFamily}"/>
        <Setter Property="FontSize" Value="{StaticResource LabelSmallFontSize}"/>
        <Setter Property="FontWeight" Value="{StaticResource LabelFontWeight}"/>
        <Setter Property="HorizontalContentAlignment" Value="Center"/>
        <Setter Property="VerticalContentAlignment" Value="Center"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="UseLayoutRounding" Value="True"/>
        <Setter Property="materialDesign:ButtonAssist.CornerRadius" Value="16"/>
        <Setter Property="materialDesign:RippleAssist.IsDisabled" Value="False"/>
        <Setter Property="materialDesign:RippleAssist.Feedback" Value="{DynamicResource OnSurfaceVariantBrush}"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type Button}">
                    <Border x:Name="border"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="16"
                            Padding="{TemplateBinding Padding}">
                        <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                        VerticalAlignment="{TemplateBinding VerticalContentAlignment}"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="border" Property="Background" Value="{DynamicResource SurfaceVariantBrush}"/>
                            <Setter Property="Foreground" Value="{DynamicResource OnSurfaceBrush}"/>
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="border" Property="Background" Value="{DynamicResource SurfaceVariantBrush}"/>
                            <Setter Property="Foreground" Value="{DynamicResource PrimaryBrush}"/>
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Opacity" Value="0.38"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- MD3 Navigation Rail Selected Item Style -->
    <Style x:Key="MD3NavigationRailSelectedItemStyle" TargetType="{x:Type Button}" BasedOn="{StaticResource MD3NavigationRailItemStyle}">
        <Setter Property="Background" Value="{DynamicResource PrimaryContainerBrush}"/>
        <Setter Property="Foreground" Value="{DynamicResource OnPrimaryContainerBrush}"/>
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background">
                    <Setter.Value>
                        <SolidColorBrush Color="#E8DDF7" Opacity="0.92"/>
                    </Setter.Value>
                </Setter>
            </Trigger>
            <Trigger Property="IsPressed" Value="True">
                <Setter Property="Background">
                    <Setter.Value>
                        <SolidColorBrush Color="#E8DDF7" Opacity="0.88"/>
                    </Setter.Value>
                </Setter>
            </Trigger>
        </Style.Triggers>
    </Style>

    <!-- MD3 Navigation Drawer Style -->
    <Style x:Key="MD3NavigationDrawerStyle" TargetType="{x:Type Border}">
        <Setter Property="Background" Value="{DynamicResource SurfaceBrush}"/>
        <Setter Property="BorderBrush" Value="{DynamicResource OutlineVariantBrush}"/>
        <Setter Property="BorderThickness" Value="0,0,1,0"/>
        <Setter Property="Width" Value="360"/>
        <Setter Property="UseLayoutRounding" Value="True"/>
        <Setter Property="Effect">
            <Setter.Value>
                <DropShadowEffect Color="Black" Opacity="0.08" BlurRadius="8" ShadowDepth="2"/>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- MD3 Navigation Drawer Header Style -->
    <Style x:Key="MD3NavigationDrawerHeaderStyle" TargetType="{x:Type Border}">
        <Setter Property="Background" Value="{DynamicResource SurfaceBrush}"/>
        <Setter Property="Padding" Value="{StaticResource SectionPadding}"/>
        <Setter Property="BorderBrush" Value="{DynamicResource OutlineVariantBrush}"/>
        <Setter Property="BorderThickness" Value="0,0,0,1"/>
        <Setter Property="UseLayoutRounding" Value="True"/>
    </Style>

    <!-- MD3 Navigation Drawer Item Style -->
    <Style x:Key="MD3NavigationDrawerItemStyle" TargetType="{x:Type Button}">
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="Foreground" Value="{DynamicResource OnSurfaceBrush}"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Height" Value="{StaticResource FABSize}"/>
        <Setter Property="Padding" Value="{StaticResource MediumSpacing}"/>
        <Setter Property="Margin" Value="{StaticResource ExtraSmallSpacing}"/>
        <Setter Property="FontFamily" Value="{StaticResource PrimaryFontFamily}"/>
        <Setter Property="FontSize" Value="{StaticResource LabelLargeFontSize}"/>
        <Setter Property="FontWeight" Value="{StaticResource LabelFontWeight}"/>
        <Setter Property="HorizontalContentAlignment" Value="Left"/>
        <Setter Property="VerticalContentAlignment" Value="Center"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="UseLayoutRounding" Value="True"/>
        <Setter Property="materialDesign:ButtonAssist.CornerRadius" Value="{StaticResource ExtraLargeCornerRadius}"/>
        <Setter Property="materialDesign:RippleAssist.IsDisabled" Value="False"/>
        <Setter Property="materialDesign:RippleAssist.Feedback" Value="{DynamicResource OnSurfaceBrush}"/>
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background" Value="{DynamicResource SurfaceVariantBrush}"/>
            </Trigger>
            <Trigger Property="IsPressed" Value="True">
                <Setter Property="Background" Value="{DynamicResource SurfaceVariantBrush}"/>
                <Setter Property="Foreground" Value="{DynamicResource PrimaryBrush}"/>
            </Trigger>
            <Trigger Property="IsEnabled" Value="False">
                <Setter Property="Opacity" Value="0.38"/>
            </Trigger>
        </Style.Triggers>
    </Style>

    <!-- MD3 Navigation Drawer Selected Item Style -->
    <Style x:Key="MD3NavigationDrawerSelectedItemStyle" TargetType="{x:Type Button}" BasedOn="{StaticResource MD3NavigationDrawerItemStyle}">
        <Setter Property="Background" Value="{DynamicResource PrimaryContainerBrush}"/>
        <Setter Property="Foreground" Value="{DynamicResource OnPrimaryContainerBrush}"/>
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background">
                    <Setter.Value>
                        <SolidColorBrush Color="#E8DDF7" Opacity="0.92"/>
                    </Setter.Value>
                </Setter>
            </Trigger>
            <Trigger Property="IsPressed" Value="True">
                <Setter Property="Background">
                    <Setter.Value>
                        <SolidColorBrush Color="#E8DDF7" Opacity="0.88"/>
                    </Setter.Value>
                </Setter>
            </Trigger>
        </Style.Triggers>
    </Style>

    <!-- MD3 Breadcrumb Style -->
    <Style x:Key="MD3BreadcrumbStyle" TargetType="{x:Type Border}">
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="Padding" Value="{StaticResource MediumSpacing}"/>
        <Setter Property="UseLayoutRounding" Value="True"/>
    </Style>

    <!-- MD3 Breadcrumb Item Style -->
    <Style x:Key="MD3BreadcrumbItemStyle" TargetType="{x:Type Button}">
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="Foreground" Value="{DynamicResource OnSurfaceVariantBrush}"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Padding" Value="{StaticResource SmallSpacing}"/>
        <Setter Property="FontFamily" Value="{StaticResource PrimaryFontFamily}"/>
        <Setter Property="FontSize" Value="{StaticResource BodyMediumFontSize}"/>
        <Setter Property="FontWeight" Value="{StaticResource BodyFontWeight}"/>
        <Setter Property="HorizontalContentAlignment" Value="Center"/>
        <Setter Property="VerticalContentAlignment" Value="Center"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="UseLayoutRounding" Value="True"/>
        <Setter Property="materialDesign:ButtonAssist.CornerRadius" Value="{StaticResource ExtraSmallCornerRadius}"/>
        <Setter Property="materialDesign:RippleAssist.IsDisabled" Value="False"/>
        <Setter Property="materialDesign:RippleAssist.Feedback" Value="{DynamicResource OnSurfaceVariantBrush}"/>
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background" Value="{DynamicResource SurfaceVariantBrush}"/>
                <Setter Property="Foreground" Value="{DynamicResource OnSurfaceBrush}"/>
            </Trigger>
            <Trigger Property="IsPressed" Value="True">
                <Setter Property="Background" Value="{DynamicResource SurfaceVariantBrush}"/>
                <Setter Property="Foreground" Value="{DynamicResource PrimaryBrush}"/>
            </Trigger>
            <Trigger Property="IsEnabled" Value="False">
                <Setter Property="Opacity" Value="0.38"/>
            </Trigger>
        </Style.Triggers>
    </Style>

    <!-- MD3 Breadcrumb Current Item Style -->
    <Style x:Key="MD3BreadcrumbCurrentItemStyle" TargetType="{x:Type TextBlock}">
        <Setter Property="Foreground" Value="{DynamicResource OnSurfaceBrush}"/>
        <Setter Property="FontFamily" Value="{StaticResource PrimaryFontFamily}"/>
        <Setter Property="FontSize" Value="{StaticResource BodyMediumFontSize}"/>
        <Setter Property="FontWeight" Value="{StaticResource BodyFontWeight}"/>
        <Setter Property="Padding" Value="{StaticResource SmallSpacing}"/>
        <Setter Property="UseLayoutRounding" Value="True"/>
    </Style>

    <!-- MD3 Breadcrumb Separator Style -->
    <Style x:Key="MD3BreadcrumbSeparatorStyle" TargetType="{x:Type TextBlock}">
        <Setter Property="Foreground" Value="{DynamicResource OnSurfaceVariantBrush}"/>
        <Setter Property="FontFamily" Value="{StaticResource PrimaryFontFamily}"/>
        <Setter Property="FontSize" Value="{StaticResource BodyMediumFontSize}"/>
        <Setter Property="FontWeight" Value="{StaticResource BodyFontWeight}"/>
        <Setter Property="Padding" Value="{StaticResource ExtraSmallSpacing}"/>
        <Setter Property="Text" Value="/"/>
        <Setter Property="UseLayoutRounding" Value="True"/>
    </Style>

    <!-- MD3 Tab Bar Style -->
    <Style x:Key="MD3TabBarStyle" TargetType="{x:Type TabControl}" BasedOn="{StaticResource MaterialDesignTabablzControlStyle}">
        <Setter Property="Background" Value="{DynamicResource SurfaceBrush}"/>
        <Setter Property="BorderBrush" Value="{DynamicResource OutlineVariantBrush}"/>
        <Setter Property="BorderThickness" Value="0,0,0,1"/>
        <Setter Property="UseLayoutRounding" Value="True"/>
        <Setter Property="materialDesign:ColorZoneAssist.Mode" Value="PrimaryMid"/>
    </Style>

    <!-- MD3 Tab Item Style -->
    <Style x:Key="MD3TabItemStyle" TargetType="{x:Type TabItem}" BasedOn="{StaticResource MaterialDesignTabItem}">
        <Setter Property="FontFamily" Value="{StaticResource PrimaryFontFamily}"/>
        <Setter Property="FontSize" Value="{StaticResource LabelLargeFontSize}"/>
        <Setter Property="FontWeight" Value="{StaticResource LabelFontWeight}"/>
        <Setter Property="Foreground" Value="{DynamicResource OnSurfaceBrush}"/>
        <Setter Property="Padding" Value="{StaticResource MediumSpacing}"/>
        <Setter Property="Height" Value="{StaticResource LargeButtonHeight}"/>
        <Setter Property="UseLayoutRounding" Value="True"/>
        <Style.Triggers>
            <Trigger Property="IsSelected" Value="True">
                <Setter Property="Foreground" Value="{DynamicResource PrimaryBrush}"/>
            </Trigger>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background" Value="{DynamicResource SurfaceVariantBrush}"/>
                <Setter Property="Foreground" Value="{DynamicResource OnSurfaceBrush}"/>
            </Trigger>
            <Trigger Property="IsEnabled" Value="False">
                <Setter Property="Opacity" Value="0.38"/>
            </Trigger>
        </Style.Triggers>
    </Style>

    <!-- MD3 Menu Style -->
    <Style x:Key="MD3MenuStyle" TargetType="{x:Type Menu}">
        <Setter Property="Background" Value="{DynamicResource SurfaceBrush}"/>
        <Setter Property="Foreground" Value="{DynamicResource OnSurfaceBrush}"/>
        <Setter Property="FontFamily" Value="{StaticResource PrimaryFontFamily}"/>
        <Setter Property="FontSize" Value="{StaticResource BodyLargeFontSize}"/>
        <Setter Property="FontWeight" Value="{StaticResource BodyFontWeight}"/>
        <Setter Property="UseLayoutRounding" Value="True"/>
    </Style>

    <!-- MD3 Menu Item Style -->
    <Style x:Key="MD3MenuItemStyle" TargetType="{x:Type MenuItem}" BasedOn="{StaticResource MaterialDesignMenuItem}">
        <Setter Property="FontFamily" Value="{StaticResource PrimaryFontFamily}"/>
        <Setter Property="FontSize" Value="{StaticResource BodyLargeFontSize}"/>
        <Setter Property="FontWeight" Value="{StaticResource BodyFontWeight}"/>
        <Setter Property="Foreground" Value="{DynamicResource OnSurfaceBrush}"/>
        <Setter Property="Height" Value="{StaticResource LargeButtonHeight}"/>
        <Setter Property="Padding" Value="{StaticResource MediumSpacing}"/>
        <Setter Property="UseLayoutRounding" Value="True"/>
        <Style.Triggers>
            <Trigger Property="IsHighlighted" Value="True">
                <Setter Property="Background" Value="{DynamicResource SurfaceVariantBrush}"/>
            </Trigger>
            <Trigger Property="IsEnabled" Value="False">
                <Setter Property="Opacity" Value="0.38"/>
            </Trigger>
        </Style.Triggers>
    </Style>

    <!-- MD3 Context Menu Style -->
    <Style x:Key="MD3ContextMenuStyle" TargetType="{x:Type ContextMenu}" BasedOn="{StaticResource MaterialDesignContextMenu}">
        <Setter Property="Background" Value="{DynamicResource SurfaceBrush}"/>
        <Setter Property="BorderBrush" Value="{DynamicResource OutlineVariantBrush}"/>
        <Setter Property="BorderThickness" Value="{StaticResource ThinBorder}"/>
        <Setter Property="Padding" Value="{StaticResource ExtraSmallSpacing}"/>
        <Setter Property="UseLayoutRounding" Value="True"/>
        <Setter Property="Effect">
            <Setter.Value>
                <DropShadowEffect Color="Black" Opacity="0.12" BlurRadius="8" ShadowDepth="4"/>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- MD3 Toolbar Style -->
    <Style x:Key="MD3ToolbarStyle" TargetType="{x:Type ToolBar}" BasedOn="{StaticResource MaterialDesignToolBar}">
        <Setter Property="Background" Value="{DynamicResource SurfaceBrush}"/>
        <Setter Property="BorderBrush" Value="{DynamicResource OutlineVariantBrush}"/>
        <Setter Property="BorderThickness" Value="0,0,0,1"/>
        <Setter Property="Padding" Value="{StaticResource MediumSpacing}"/>
        <Setter Property="UseLayoutRounding" Value="True"/>
        <Setter Property="Effect">
            <Setter.Value>
                <DropShadowEffect Color="Black" Opacity="0.04" BlurRadius="2" ShadowDepth="1"/>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- Legacy Compatibility Aliases -->
    <Style x:Key="NavigationRailStyle" TargetType="{x:Type Border}" BasedOn="{StaticResource MD3NavigationRailStyle}"/>
    <Style x:Key="NavigationRailItemStyle" TargetType="{x:Type Button}" BasedOn="{StaticResource MD3NavigationRailItemStyle}"/>
    <Style x:Key="NavigationDrawerStyle" TargetType="{x:Type Border}" BasedOn="{StaticResource MD3NavigationDrawerStyle}"/>
    <Style x:Key="NavigationDrawerItemStyle" TargetType="{x:Type Button}" BasedOn="{StaticResource MD3NavigationDrawerItemStyle}"/>
    <Style x:Key="BreadcrumbStyle" TargetType="{x:Type Border}" BasedOn="{StaticResource MD3BreadcrumbStyle}"/>
    <Style x:Key="BreadcrumbItemStyle" TargetType="{x:Type Button}" BasedOn="{StaticResource MD3BreadcrumbItemStyle}"/>
    <Style x:Key="TabBarStyle" TargetType="{x:Type TabControl}" BasedOn="{StaticResource MD3TabBarStyle}"/>
    <Style x:Key="TabItemStyle" TargetType="{x:Type TabItem}" BasedOn="{StaticResource MD3TabItemStyle}"/>
    <Style x:Key="MenuStyle" TargetType="{x:Type Menu}" BasedOn="{StaticResource MD3MenuStyle}"/>
    <Style x:Key="MenuItemStyle" TargetType="{x:Type MenuItem}" BasedOn="{StaticResource MD3MenuItemStyle}"/>
    <Style x:Key="ContextMenuStyle" TargetType="{x:Type ContextMenu}" BasedOn="{StaticResource MD3ContextMenuStyle}"/>
    <Style x:Key="ToolbarStyle" TargetType="{x:Type ToolBar}" BasedOn="{StaticResource MD3ToolbarStyle}"/>

</ResourceDictionary>