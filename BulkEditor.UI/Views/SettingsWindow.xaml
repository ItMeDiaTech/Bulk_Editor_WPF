<Window x:Class="BulkEditor.UI.Views.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:views="clr-namespace:BulkEditor.UI.Views.Settings"
        mc:Ignorable="d"
        Title="Settings - Bulk Document Editor"
        Height="600" Width="800"
        WindowStartupLocation="CenterOwner"
        ResizeMode="CanResize"
        MinHeight="500" MinWidth="700">

    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
        <Style TargetType="GroupBox">
            <Setter Property="Margin" Value="8"/>
            <Setter Property="Padding" Value="12"/>
            <Setter Property="BorderBrush" Value="#E0E0E0"/>
            <Setter Property="BorderThickness" Value="1"/>
        </Style>
        <Style TargetType="TextBlock" x:Key="SectionHeader">
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Margin" Value="0,0,0,8"/>
            <Setter Property="Foreground" Value="#2196F3"/>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Settings Content -->
        <TabControl Grid.Row="0" Margin="16">

            <!-- General Settings -->
            <TabItem Header="General">
                <views:ProcessingSettingsView DataContext="{Binding ProcessingSettings}"/>
            </TabItem>

            <!-- Validation Settings -->
            <TabItem Header="Validation">
                <views:ValidationSettingsView DataContext="{Binding ValidationSettings}"/>
            </TabItem>

            <!-- Backup Settings -->
            <TabItem Header="Backup">
                <views:BackupSettingsView DataContext="{Binding BackupSettings}"/>
            </TabItem>

            <!-- Logging Settings -->
            <TabItem Header="Logging">
                <views:LoggingSettingsView DataContext="{Binding LoggingSettings}"/>
            </TabItem>

            <!-- Update Settings -->
            <TabItem Header="Updates">
                <views:UpdateSettingsView DataContext="{Binding UpdateSettings}"/>
            </TabItem>

        </TabControl>

        <!-- Action Buttons -->
        <Border Grid.Row="1" Background="#F5F5F5" Padding="16,12">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="Reset to Defaults"
                        Command="{Binding ResetToDefaultsCommand}"
                        Margin="0,0,8,0"
                        Padding="16,8"/>
                <Button Content="Cancel"
                        Command="{Binding CancelCommand}"
                        Margin="0,0,8,0"
                        Padding="16,8"/>
                <Button Content="Save Settings"
                        Command="{Binding SaveSettingsCommand}"
                        Background="#2196F3"
                        Foreground="White"
                        Padding="16,8"/>
            </StackPanel>
        </Border>

    </Grid>
</Window>