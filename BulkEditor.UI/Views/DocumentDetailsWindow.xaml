<Window x:Class="BulkEditor.UI.Views.DocumentDetailsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:BulkEditor.UI.ViewModels"
        mc:Ignorable="d"
        Title="{Binding WindowTitle}"
        Height="700" Width="800"
        MinHeight="600" MinWidth="700"
        WindowStartupLocation="CenterOwner"
        ResizeMode="CanResize"
        Background="#F8F9FA">

    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVisConverter"/>
        
        <!-- Field Label Style -->
        <Style x:Key="FieldLabelStyle" TargetType="TextBlock">
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Foreground" Value="#333333"/>
            <Setter Property="Margin" Value="0,0,8,4"/>
        </Style>
        
        <!-- Field Value Style -->
        <Style x:Key="FieldValueStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Foreground" Value="#666666"/>
            <Setter Property="TextWrapping" Value="Wrap"/>
            <Setter Property="Margin" Value="0,0,0,12"/>
        </Style>
        
        <!-- Section Header Style -->
        <Style x:Key="SectionHeaderStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Foreground" Value="#2196F3"/>
            <Setter Property="Margin" Value="0,16,0,8"/>
        </Style>
        
        <!-- Action Button Style -->
        <Style x:Key="ActionButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#2196F3"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="12,8"/>
            <Setter Property="Margin" Value="4"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="4"
                                Padding="{TemplateBinding Padding}">
                            <TextBlock Text="{TemplateBinding Content}"
                                       HorizontalAlignment="Center"
                                       VerticalAlignment="Center"
                                       Foreground="{TemplateBinding Foreground}"
                                       FontSize="{TemplateBinding FontSize}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#1976D2"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Background" Value="#BDBDBD"/>
                                <Setter Property="Foreground" Value="#757575"/>
                                <Setter Property="Cursor" Value="Arrow"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        
        <!-- Special Action Button Styles -->
        <Style x:Key="DangerButtonStyle" TargetType="Button" BasedOn="{StaticResource ActionButtonStyle}">
            <Setter Property="Background" Value="#F44336"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#D32F2F"/>
                </Trigger>
            </Style.Triggers>
        </Style>
        
        <Style x:Key="WarningButtonStyle" TargetType="Button" BasedOn="{StaticResource ActionButtonStyle}">
            <Setter Property="Background" Value="#FF9800"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#F57C00"/>
                </Trigger>
            </Style.Triggers>
        </Style>
        
        <Style x:Key="SuccessButtonStyle" TargetType="Button" BasedOn="{StaticResource ActionButtonStyle}">
            <Setter Property="Background" Value="#4CAF50"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#388E3C"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Background="#2196F3" Padding="20,16">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                
                <StackPanel Grid.Column="0">
                    <TextBlock Text="{Binding DocumentTitle}"
                               FontSize="20"
                               FontWeight="Bold"
                               Foreground="White"
                               TextTrimming="CharacterEllipsis"/>
                    <TextBlock Text="{Binding DocumentPath}"
                               FontSize="12"
                               Foreground="#E3F2FD"
                               TextTrimming="CharacterEllipsis"
                               Margin="0,4,0,0"/>
                </StackPanel>
                
                <StackPanel Grid.Column="1" Orientation="Horizontal">
                    <!-- Status Display -->
                    <Border Background="{Binding StatusBackgroundColor}"
                            CornerRadius="12"
                            Padding="12,6"
                            Margin="0,0,16,0">
                        <TextBlock Text="{Binding StatusDisplayText}"
                                   Foreground="White"
                                   FontSize="11"
                                   FontWeight="Bold"
                                   HorizontalAlignment="Center"
                                   VerticalAlignment="Center"/>
                    </Border>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Main Content -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" Padding="20">
            <StackPanel>
                
                <!-- Document Information Section -->
                <TextBlock Text="Document Information" Style="{StaticResource SectionHeaderStyle}"/>
                
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="120"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    
                    <TextBlock Grid.Row="0" Grid.Column="0" Text="File Name:" Style="{StaticResource FieldLabelStyle}"/>
                    <TextBlock Grid.Row="0" Grid.Column="1" Text="{Binding DocumentTitle}" Style="{StaticResource FieldValueStyle}"/>
                    
                    <TextBlock Grid.Row="1" Grid.Column="0" Text="Location:" Style="{StaticResource FieldLabelStyle}"/>
                    <TextBlock Grid.Row="1" Grid.Column="1" Text="{Binding DocumentPath}" Style="{StaticResource FieldValueStyle}"/>
                    
                    <TextBlock Grid.Row="2" Grid.Column="0" Text="Status:" Style="{StaticResource FieldLabelStyle}"/>
                    <TextBlock Grid.Row="2" Grid.Column="1" Text="{Binding StatusDisplayText}" Style="{StaticResource FieldValueStyle}"/>
                    
                    <TextBlock Grid.Row="3" Grid.Column="0" Text="Processing Time:" Style="{StaticResource FieldLabelStyle}"/>
                    <TextBlock Grid.Row="3" Grid.Column="1" Text="{Binding ProcessingTimeDisplay}" Style="{StaticResource FieldValueStyle}"/>
                    
                    <TextBlock Grid.Row="4" Grid.Column="0" Text="Processed At:" Style="{StaticResource FieldLabelStyle}"/>
                    <TextBlock Grid.Row="4" Grid.Column="1" Text="{Binding ProcessedAtDisplay}" Style="{StaticResource FieldValueStyle}"/>
                </Grid>

                <!-- Processing Statistics Section -->
                <TextBlock Text="Processing Statistics" Style="{StaticResource SectionHeaderStyle}"
                          Visibility="{Binding HasProcessingResults, Converter={StaticResource BoolToVisConverter}}"/>
                
                <Grid Visibility="{Binding HasProcessingResults, Converter={StaticResource BoolToVisConverter}}">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="120"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    
                    <TextBlock Grid.Row="0" Grid.Column="0" Text="Total Changes:" Style="{StaticResource FieldLabelStyle}"/>
                    <TextBlock Grid.Row="0" Grid.Column="1" Text="{Binding TotalChangesCount}" Style="{StaticResource FieldValueStyle}"/>
                    
                    <TextBlock Grid.Row="1" Grid.Column="0" Text="Hyperlinks:" Style="{StaticResource FieldLabelStyle}"/>
                    <TextBlock Grid.Row="1" Grid.Column="1" Text="{Binding HyperlinkChangesCount}" Style="{StaticResource FieldValueStyle}"/>
                    
                    <TextBlock Grid.Row="2" Grid.Column="0" Text="Title Changes:" Style="{StaticResource FieldLabelStyle}"/>
                    <TextBlock Grid.Row="2" Grid.Column="1" Text="{Binding TitleChangesCount}" Style="{StaticResource FieldValueStyle}"/>
                    
                    <TextBlock Grid.Row="3" Grid.Column="0" Text="Errors:" Style="{StaticResource FieldLabelStyle}"/>
                    <TextBlock Grid.Row="3" Grid.Column="1" Text="{Binding ErrorCount}" Style="{StaticResource FieldValueStyle}"/>
                </Grid>

                <!-- Processing Details Section -->
                <TextBlock Text="Processing Details" Style="{StaticResource SectionHeaderStyle}"
                          Visibility="{Binding HasProcessingResults, Converter={StaticResource BoolToVisConverter}}"/>
                
                <ItemsControl ItemsSource="{Binding ProcessingOptions}"
                              Visibility="{Binding HasProcessingResults, Converter={StaticResource BoolToVisConverter}}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Background="White"
                                    BorderBrush="#E0E0E0"
                                    BorderThickness="1"
                                    CornerRadius="4"
                                    Margin="0,0,0,8"
                                    Padding="16">
                                <StackPanel>
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>
                                        
                                        <TextBlock Grid.Column="0"
                                                   Text="{Binding OptionName}"
                                                   FontWeight="Bold"
                                                   FontSize="13"
                                                   Foreground="#444444"/>
                                        
                                        <StackPanel Grid.Column="1" Orientation="Horizontal">
                                            <CheckBox IsChecked="{Binding WasProcessed, Mode=OneWay}"
                                                      IsEnabled="False"
                                                      VerticalAlignment="Center"
                                                      Margin="8,0"/>
                                            <Border Background="#E0E0E0"
                                                    CornerRadius="10"
                                                    Padding="6,2"
                                                    Visibility="{Binding HasChanges, Converter={StaticResource BoolToVisConverter}}">
                                                <TextBlock Text="{Binding ChangeCount, StringFormat='{}{0} changes'}"
                                                           FontSize="10"
                                                           Foreground="#666666"/>
                                            </Border>
                                        </StackPanel>
                                    </Grid>
                                    
                                    <!-- Changes List -->
                                    <ItemsControl ItemsSource="{Binding Changes}"
                                                  Margin="0,8,0,0"
                                                  Visibility="{Binding HasChanges, Converter={StaticResource BoolToVisConverter}}">
                                        <ItemsControl.ItemTemplate>
                                            <DataTemplate>
                                                <Border Background="#FAFAFA"
                                                        BorderBrush="#E0E0E0"
                                                        BorderThickness="1"
                                                        CornerRadius="4"
                                                        Padding="12"
                                                        Margin="0,2">
                                                    <Grid>
                                                        <Grid.RowDefinitions>
                                                            <RowDefinition Height="Auto"/>
                                                            <RowDefinition Height="Auto"/>
                                                            <RowDefinition Height="Auto"/>
                                                            <RowDefinition Height="Auto"/>
                                                            <RowDefinition Height="Auto"/>
                                                        </Grid.RowDefinitions>
                                                        <Grid.ColumnDefinitions>
                                                            <ColumnDefinition Width="120"/>
                                                            <ColumnDefinition Width="*"/>
                                                        </Grid.ColumnDefinitions>
                                                        
                                                        <TextBlock Grid.Row="0" Grid.Column="0" Text="Current Title:" FontWeight="Bold" FontSize="11"/>
                                                        <TextBlock Grid.Row="0" Grid.Column="1" Text="{Binding CurrentTitle}" FontSize="11" TextWrapping="Wrap"/>
                                                        
                                                        <TextBlock Grid.Row="1" Grid.Column="0" Text="Preprocessed URL:" FontWeight="Bold" FontSize="11" Margin="0,4,0,0"/>
                                                        <TextBlock Grid.Row="1" Grid.Column="1" Text="{Binding PreprocessedUrl}" FontSize="11" TextWrapping="Wrap" Margin="0,4,0,0"/>
                                                        
                                                        <TextBlock Grid.Row="2" Grid.Column="0" Text="New URL:" FontWeight="Bold" FontSize="11" Margin="0,4,0,0"/>
                                                        <TextBlock Grid.Row="2" Grid.Column="1" Text="{Binding NewUrl}" FontSize="11" TextWrapping="Wrap" Margin="0,4,0,0"/>
                                                        
                                                        <TextBlock Grid.Row="3" Grid.Column="0" Text="Content ID:" FontWeight="Bold" FontSize="11" Margin="0,4,0,0"/>
                                                        <TextBlock Grid.Row="3" Grid.Column="1" Text="{Binding ContentId}" FontSize="11" Margin="0,4,0,0"/>
                                                        
                                                        <TextBlock Grid.Row="4" Grid.Column="0" Text="Change:" FontWeight="Bold" FontSize="11" Margin="0,4,0,0"/>
                                                        <Border Grid.Row="4" Grid.Column="1"
                                                                Background="#2196F3"
                                                                CornerRadius="12"
                                                                Padding="8,2"
                                                                HorizontalAlignment="Left"
                                                                Margin="0,4,0,0">
                                                            <TextBlock Text="{Binding ChangeType}"
                                                                       Foreground="White"
                                                                       FontSize="10"
                                                                       FontWeight="Bold"/>
                                                        </Border>
                                                    </Grid>
                                                </Border>
                                            </DataTemplate>
                                        </ItemsControl.ItemTemplate>
                                    </ItemsControl>
                                </StackPanel>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
                
            </StackPanel>
        </ScrollViewer>

        <!-- Action Buttons -->
        <Border Grid.Row="2" Background="#F0F0F0" Padding="20,16" BorderBrush="#E0E0E0" BorderThickness="0,1,0,0">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                
                <!-- Left side buttons -->
                <StackPanel Grid.Column="0" Orientation="Horizontal">
                    <Button Content="Open Document"
                            Command="{Binding OpenDocumentCommand}"
                            Style="{StaticResource ActionButtonStyle}"/>
                    <Button Content="Open File Location"
                            Command="{Binding OpenFileLocationCommand}"
                            Style="{StaticResource ActionButtonStyle}"/>
                    <Button Content="View Backup File"
                            Command="{Binding ViewBackupFileCommand}"
                            IsEnabled="{Binding HasBackup}"
                            Style="{StaticResource ActionButtonStyle}"/>
                    <Button Content="Revert All Changes"
                            Command="{Binding RevertAllChangesCommand}"
                            IsEnabled="{Binding CanRevert}"
                            Style="{StaticResource WarningButtonStyle}"/>
                </StackPanel>
                
                <!-- Right side button -->
                <Button Grid.Column="1"
                        Content="Close"
                        Command="{Binding CloseCommand}"
                        Style="{StaticResource ActionButtonStyle}"/>
            </Grid>
        </Border>
    </Grid>
</Window>