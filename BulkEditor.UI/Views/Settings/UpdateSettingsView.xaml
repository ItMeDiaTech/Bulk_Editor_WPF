<UserControl x:Class="BulkEditor.UI.Views.Settings.UpdateSettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
    </UserControl.Resources>
    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Margin="16">

            <!-- Auto Update Settings -->
            <GroupBox Header="Automatic Updates">
                <StackPanel>
                    <CheckBox Content="Enable automatic updates"
                              IsChecked="{Binding AutoUpdateEnabled}"
                              Margin="0,0,0,8"/>
                    <CheckBox Content="Install security updates automatically"
                              IsChecked="{Binding InstallSecurityUpdatesAutomatically}"
                              Margin="0,0,0,8"/>
                    <CheckBox Content="Notify when updates are available"
                              IsChecked="{Binding NotifyOnUpdatesAvailable}"
                              Margin="0,0,0,8"/>
                    <CheckBox Content="Create backup before updating"
                              IsChecked="{Binding CreateBackupBeforeUpdate}"
                              Margin="0,0,0,8"/>
                    <CheckBox Content="Include pre-release versions"
                              IsChecked="{Binding IncludePrerelease}"
                              Margin="0,0,0,8"/>

                    <Grid Margin="0,0,0,8">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="150"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="Check interval (hours):" VerticalAlignment="Center"/>
                        <TextBox Grid.Column="1" Text="{Binding CheckIntervalHours}"
                                 HorizontalAlignment="Stretch"/>
                    </Grid>
                </StackPanel>
            </GroupBox>

            <!-- GitHub Repository Settings -->
            <GroupBox Header="Override Repository Settings">
                <StackPanel>
                    <TextBlock Text="Application defaults: ItMeDiaTech / Bulk_Editor_WPF"
                               FontStyle="Italic" 
                               Foreground="Gray" 
                               Margin="0,0,0,8"/>
                    <TextBlock Text="Only modify these fields to override the default application settings."
                               FontStyle="Italic" 
                               Foreground="Orange" 
                               FontWeight="SemiBold"
                               Margin="0,0,0,12"/>
                               
                    <Grid Margin="0,0,0,8">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="200"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="GitHub Owner:" VerticalAlignment="Center"/>
                        <TextBox Grid.Column="1" Text="{Binding GitHubOwner}"
                                 HorizontalAlignment="Stretch"/>
                    </Grid>

                    <Grid Margin="0,0,0,8">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="200"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="GitHub Repository:" VerticalAlignment="Center"/>
                        <TextBox Grid.Column="1" Text="{Binding GitHubRepository}"
                                 HorizontalAlignment="Stretch"/>
                    </Grid>
                </StackPanel>
            </GroupBox>

            <!-- Version Information -->
            <GroupBox Header="Version Information">
                <StackPanel>
                    <Grid Margin="0,0,0,8">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="150"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="Current Version:" VerticalAlignment="Center" FontWeight="SemiBold"/>
                        <TextBlock Grid.Column="1" Text="{Binding CurrentVersion}"
                                   HorizontalAlignment="Left" VerticalAlignment="Center"/>
                    </Grid>

                    <Grid Margin="0,0,0,8">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="150"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="Latest Version:" VerticalAlignment="Center" FontWeight="SemiBold"/>
                        <TextBlock Grid.Column="1" Text="{Binding LatestVersion}"
                                   HorizontalAlignment="Left" VerticalAlignment="Center"/>
                    </Grid>

                    <TextBlock Text="{Binding UpdateCheckStatus}"
                               FontStyle="Italic"
                               Foreground="Gray"
                               Margin="0,8,0,8"
                               TextWrapping="Wrap"/>

                    <Border Background="#F0F8FF" BorderBrush="#2196F3" BorderThickness="1"
                            CornerRadius="4" Padding="12" Margin="0,8,0,8"
                            Visibility="{Binding UpdateAvailable, Converter={StaticResource BoolToVisibilityConverter}}">
                        <StackPanel>
                            <TextBlock Text="Update Available!" FontWeight="Bold" Foreground="#2196F3" Margin="0,0,0,4"/>
                            <TextBlock Text="{Binding ReleaseNotes}" TextWrapping="Wrap" FontSize="12"/>
                        </StackPanel>
                    </Border>

                    <Grid Margin="0,8,0,0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <ProgressBar Grid.Column="0" IsIndeterminate="True" Height="20"
                                     Visibility="{Binding IsBusy, Converter={StaticResource BoolToVisibilityConverter}}"/>
                        <TextBlock Grid.Column="0" Text="{Binding BusyMessage}"
                                   VerticalAlignment="Center" HorizontalAlignment="Center"
                                   Visibility="{Binding IsBusy, Converter={StaticResource BoolToVisibilityConverter}}"/>
                        <Button Grid.Column="1" Content="Check for Updates"
                                Command="{Binding CheckForUpdatesCommand}"
                                Padding="16,8" Margin="8,0,0,0">
                            <Button.Style>
                                <Style TargetType="Button">
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding IsBusy}" Value="True">
                                            <Setter Property="IsEnabled" Value="False"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Button.Style>
                        </Button>
                    </Grid>
                </StackPanel>
            </GroupBox>

        </StackPanel>
    </ScrollViewer>
</UserControl>