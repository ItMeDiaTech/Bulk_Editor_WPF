<Window x:Class="BulkEditor.UI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:controls="clr-namespace:BulkEditor.UI.Controls"
        xmlns:local="clr-namespace:BulkEditor.UI.Converters"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        mc:Ignorable="d"
        Title="{Binding Title}"
        Height="800" Width="1400"
        MinHeight="700" MinWidth="1100"
        WindowStartupLocation="CenterScreen"
        Background="{DynamicResource BackgroundBrush}"
        TextElement.Foreground="{DynamicResource OnBackgroundBrush}"
        TextElement.FontWeight="{DynamicResource BodyFontWeight}"
        TextElement.FontSize="{DynamicResource BodyFontSize}"
        TextElement.FontFamily="{DynamicResource AppFontFamily}"
        TextOptions.TextFormattingMode="Ideal"
        TextOptions.TextRenderingMode="Auto"
        materialDesign:ElevationAssist.Elevation="Dp4">

    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVisibilityConverter" />
        <local:CountToVisibilityConverter x:Key="CountToVisibilityConverter" />
    </Window.Resources>

    <!-- Simplified Working MaterialDesign UI -->
    <Grid Background="{DynamicResource BackgroundBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Background="{DynamicResource PrimaryBrush}" Padding="20">
            <StackPanel>
                <TextBlock Text="Bulk Document Editor" 
                          FontSize="24" FontWeight="Bold" 
                          Foreground="White" 
                          HorizontalAlignment="Center"/>
                <TextBlock Text="Enterprise document processing with advanced hyperlink management" 
                          FontSize="14" 
                          Foreground="White" 
                          HorizontalAlignment="Center" 
                          Opacity="0.9" 
                          Margin="0,5,0,0"/>
            </StackPanel>
        </Border>

        <!-- Main Content -->
        <Grid Grid.Row="1" Margin="20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="300"/>
            </Grid.ColumnDefinitions>

            <!-- Document List Area -->
            <Border Grid.Column="0" 
                    Background="White" 
                    BorderBrush="#E0E0E0" 
                    BorderThickness="1" 
                    CornerRadius="4" 
                    Padding="20" 
                    Margin="0,0,10,0">
                <StackPanel>
                    <TextBlock Text="Documents" FontSize="18" FontWeight="Bold" Margin="0,0,0,10"/>
                    <TextBlock Text="Drag and drop Word documents here or use the Select Files button" 
                              FontSize="14" 
                              Foreground="Gray" 
                              HorizontalAlignment="Center" 
                              VerticalAlignment="Center"/>
                </StackPanel>
            </Border>

            <!-- Control Panel -->
            <Border Grid.Column="1" 
                    Background="White" 
                    BorderBrush="#E0E0E0" 
                    BorderThickness="1" 
                    CornerRadius="4" 
                    Padding="20" 
                    Margin="10,0,0,0">
                <StackPanel>
                    <TextBlock Text="Actions" FontSize="18" FontWeight="Bold" Margin="0,0,0,15"/>
                    
                    <Button Content="SELECT FILES" 
                            Command="{Binding SelectFilesCommand}" 
                            Background="{DynamicResource SuccessBrush}" 
                            Foreground="White" 
                            Padding="15,10" 
                            Margin="0,0,0,10" 
                            BorderThickness="0"/>
                    
                    <Button Content="PROCESS DOCUMENTS" 
                            Command="{Binding ProcessDocumentsCommand}" 
                            Background="{DynamicResource PrimaryBrush}" 
                            Foreground="White" 
                            Padding="15,10" 
                            Margin="0,0,0,10" 
                            BorderThickness="0"/>
                    
                    <Button Content="SETTINGS" 
                            Command="{Binding OpenSettingsCommand}" 
                            Background="{DynamicResource InfoBrush}" 
                            Foreground="White" 
                            Padding="15,10" 
                            Margin="0,0,0,10" 
                            BorderThickness="0"/>
                    
                    <Button Content="CLEAR ALL" 
                            Command="{Binding ClearDocumentsCommand}" 
                            Background="{DynamicResource WarningBrush}" 
                            Foreground="White" 
                            Padding="15,10" 
                            BorderThickness="0"/>
                </StackPanel>
            </Border>
        </Grid>

        <!-- Status Bar -->
        <Border Grid.Row="2" Background="#F5F5F5" Padding="10">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                
                <TextBlock Grid.Column="0" 
                          Text="{Binding StatusMessage}" 
                          VerticalAlignment="Center"/>
                
                <StackPanel Grid.Column="1" Orientation="Horizontal">
                    <TextBlock Text="{Binding TotalDocuments, StringFormat='{}{0} documents'}" 
                              Margin="0,0,15,0" 
                              VerticalAlignment="Center"/>
                    <TextBlock Text="{Binding ApplicationVersion}" 
                              VerticalAlignment="Center"/>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</Window>